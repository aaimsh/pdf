{
  "name": "pdf-toolkit",
  "version": "1.0.0",
  "private": true,
  "description": "Client-side PDF toolkit web app (dev tooling only — the site itself has no build step).",
  "scripts": {
    "serve": "python3 -m http.server 8000",
    "test": "playwright test",
    "test:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.56.1"
  }
}
